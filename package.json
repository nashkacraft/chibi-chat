{
  "name": "chibi-chat",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "prettier": "^2.8.8",
    "eslint": "^8.48.0"
  },
  "scripts": {
    "format": "prettier --write \"**/*.{html,css,js,json,md}\"",
    "lint": "eslint . --ext .js",
    "prepare": "mkdir -p public/assets && cp -r task/assets/* public/assets/ || true",
    "start": "npm run prepare && python3 -m http.server 8000 --directory public"
  }
}
